# Copy to .env.local and fill in real values. .env.local is gitignored.

MAKE_WEBHOOK_URL=https://hook.us2.make.com/your-webhook-id
META_DATASET_ID=your-meta-dataset-id
META_ACCESS_TOKEN=your-meta-conversions-api-access-token

# Same dataset ID, exposed to the browser for the Meta Pixel base code.
NEXT_PUBLIC_META_DATASET_ID=your-meta-dataset-id
